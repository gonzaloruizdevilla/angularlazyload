extends layout

block content
    #failLateRegister(ng-app="failLateRegister", ng-controller="DefaultCtrl")
        .bs-callout.bs-callout-info
          h2 Late register (fail)
          p By default, you can't register new artifacts in AngularJS after starting an application.
        table.table
            tr
                td: button(ng-click="useService1()") Usar Service1:
                td: span {{resultadoServicio1}}
            tr
                td: button(ng-click="registerService2()") Register Service2
                td: span {{registrado}}
            tr
                td: button(ng-click="useService2()") Usar Service2:
                td: span {{resultadoServicio2}}
    script(src="/javascripts/fail-late-register.js")
